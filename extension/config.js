// Configuração da extensão — preencha com os dados do seu projeto Supabase
// e a chave da API do Gemini. Este arquivo é carregado por background.js,
// content.js e popup.js (via importScripts / <script>).

const CONFIG = {
  SUPABASE_URL:      'https://SEU_PROJETO.supabase.co',
  SUPABASE_ANON_KEY: 'SUA_ANON_KEY',

  // Gerar em https://aistudio.google.com/app/apikey
  GEMINI_API_KEY:    'SUA_GEMINI_API_KEY',
  GEMINI_MODEL:      'gemini-2.0-flash',

  // URL do sistema (para abrir o lead direto no CRM)
  APP_URL: 'https://SEU_DOMINIO_DO_SISTEMA',
}
